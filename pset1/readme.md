If you want to build (and run from scratch)

$ make clean

$ make all

to run all c benchmarks
$ run_c_scripts.sh 
to run just python benchmarks
$ run_py_scripts.sh

and all the output for dp1-dp5 (with both test cases) should be in the STDOUT folder